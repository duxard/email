1) npm install
2) комманди gulp по дефолту выполняет слежение
3) html исходник лежит в templates
4) Конечные исходники буду в dist
5) Для компиляции проекта: gulp inky

*******

Чтоб медиа запросы добавились в конечный файл необходимо прописать специальный аттрибут inline, иначе письмо не будет резиновым:

<link rel="stylesheet" href="../css/styles.css" inline>